<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="play" transaction-type="RESOURCE_LOCAL">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Identity -->
        <class>com.company.foodapp.auth.User</class>

        <!-- Attendance -->
        <class>com.company.foodapp.attendance.ClockInRecord</class>

        <!-- Vendor & Menu -->
        <class>com.company.foodapp.vendor.Vendor</class>
        <class>com.company.foodapp.menu.Menu</class>
        <class>com.company.foodapp.menu.MenuItem</class>

        <!-- Ordering -->
        <class>com.company.foodapp.order.Order</class>
        <class>com.company.foodapp.order.OrderItem</class>

        <properties>

            <!-- JDBC -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver"/>

            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/play_db?useSSL=false&amp;serverTimezone=UTC"/>

            <property name="jakarta.persistence.jdbc.user"
                      value="root"/>

            <property name="jakarta.persistence.jdbc.password"
                      value="cipher108!"/>

            <!-- Hibernate -->

            <property name="hibernate.hbm2ddl.auto"
                      value="update"/>

            <property name="hibernate.show_sql"
                      value="true"/>

            <property name="hibernate.format_sql"
                      value="true"/>

        </properties>

    </persistence-unit>
</persistence>
